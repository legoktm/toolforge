language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install: "pip install tox"
# command to run tests
script: tox
notifications:
  email: false
  irc: 
    channels:
      - "chat.freenode.net##legoktm"
    skip_join: true
    on_success: change
    on_failure: always
